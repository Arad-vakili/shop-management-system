from setuptools import setup, find_packages

setup(
    name="shop-management-system",
    version="1.0.0",
    packages=find_packages(),
    python_requires=">=3.6",
    author="Your Name",
    description="A Python-based shop management system with order processing, discounts, and JSON storage",
    license="MIT",
)